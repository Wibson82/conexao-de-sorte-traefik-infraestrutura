# =============================================================================
# CONFIGURAÇÃO DO TRAEFIK - EXEMPLO
# =============================================================================
# Copie este arquivo para .env e ajuste as variáveis conforme necessário

# -----------------------------------------------------------------------------
# DOMÍNIO E CERTIFICADOS
# -----------------------------------------------------------------------------
# Domínio principal para o Traefik
DOMAIN=conexaodesorte.com.br

# Email para Let's Encrypt (obrigatório para certificados SSL)
ACME_EMAIL=facilitaservicos.dev@gmail.com

# -----------------------------------------------------------------------------
# AMBIENTE E VERSÕES
# -----------------------------------------------------------------------------
# Ambiente de execução (production, staging, development)
ENVIRONMENT=production

# Versão do Traefik
VERSION=latest

# -----------------------------------------------------------------------------
# LOGS E MONITORAMENTO
# -----------------------------------------------------------------------------
# Nível de log geral (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# Nível de log específico do Traefik (DEBUG, INFO, WARN, ERROR)
TRAEFIK_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# DEPLOY E CI/CD (Configurado automaticamente pelo pipeline)
# -----------------------------------------------------------------------------
# Timestamp do deploy (preenchido automaticamente)
# DEPLOY_TIMESTAMP=2024-01-01T00:00:00Z

# Commit hash do deploy (preenchido automaticamente)
# DEPLOY_COMMIT=abc123

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES OPCIONAIS
# -----------------------------------------------------------------------------
# Timezone
TZ=America/Sao_Paulo

# Configurações Docker
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# -----------------------------------------------------------------------------
# SECRETS PARA GITHUB ACTIONS
# -----------------------------------------------------------------------------
# As seguintes variáveis devem ser configuradas como secrets no GitHub:
# 
# SSH_HOST=seu-servidor.com
# SSH_USER=seu-usuario
# VPS_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----
#             sua-chave-ssh-privada-aqui
#             -----END OPENSSH PRIVATE KEY-----
#
# Para configurar os secrets:
# 1. Vá para Settings > Secrets and variables > Actions no seu repositório
# 2. Clique em "New repository secret"
# 3. Adicione cada secret com o nome exato mostrado acima

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE REDE
# -----------------------------------------------------------------------------
# Nome da rede Docker (usado pelos serviços que se conectam ao Traefik)
NETWORK_NAME=conexao-network

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE SEGURANÇA
# -----------------------------------------------------------------------------
# Habilitar dashboard do Traefik (true/false)
DASHBOARD_ENABLED=true

# Habilitar API do Traefik (true/false)
API_ENABLED=true

# Habilitar modo inseguro para desenvolvimento (true/false)
# ATENÇÃO: Nunca use true em produção!
INSECURE_MODE=false