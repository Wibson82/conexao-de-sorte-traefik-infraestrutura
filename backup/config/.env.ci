# ===============================================================================
# 🌐 VARIÁVEIS DE AMBIENTE PARA CI/CD - CONEXÃO DE SORTE TRAEFIK
# ===============================================================================
# Este arquivo define as configurações padrão para o ambiente de CI/CD
# Usado durante o deploy automático no servidor self-hosted

# Timezone
TZ=America/Sao_Paulo

# SSL/ACME Configuration
TRAEFIK_ACME_EMAIL=facilitaservicos.tec@gmail.com

# Domain Configuration
TRAEFIK_DOMAIN=traefik.conexaodesorte.com.br
API_DOMAIN=api.conexaodesorte.com.br

# Network Configuration (IMPORTANTE: usar rede overlay existente)
DOCKER_NETWORK_NAME=conexao-network-swarm

# Backend Legacy Configuration (se necessário)
BACKEND_SERVICE=backend-prod
BACKEND_PORT=8080

# Dashboard Configuration
ENABLE_DASHBOARD=true
API_INSECURE=false

# Logging Configuration
LOG_LEVEL=INFO
ACCESS_LOG_ENABLED=true

# Performance Configuration
HTTP3_ENABLED=true
COMPRESSION_ENABLED=true

# Security Configuration
ENABLE_HSTS=true
HSTS_MAX_AGE=31536000

# Rate Limiting Configuration
RATE_LIMIT_AVERAGE=100
RATE_LIMIT_BURST=200

# ===============================================================================
# OBSERVAÇÕES IMPORTANTES:
# ===============================================================================
# 1. DOCKER_NETWORK_NAME está configurado para usar a rede overlay 'conexao-network-swarm'
#    que já existe no servidor self-hosted conforme docker network ls
# 2. Este arquivo é específico para o ambiente de CI/CD
# 3. Para desenvolvimento local, use o arquivo .env.example como base
# 4. As redes disponíveis no servidor são:
#    - conexao-network (bridge)
#    - conexao-network-swarm (overlay) ← ESTA É USADA NO CI/CD
#    - conexao-frontend_conexao-network (overlay)
# ===============================================================================