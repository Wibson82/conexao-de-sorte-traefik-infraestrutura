name: "🚀 Deploy Unificado - Conexão de Sorte"

on:
  workflow_dispatch:
    inputs:
      deploy_type:
        description: 'Tipo de deploy'
        required: true
        type: choice
        options: ['prod', 'teste']
        default: 'teste'
      force_deploy:
        description: 'Forçar deploy mesmo sem mudanças'
        required: false
        type: boolean
        default: false
  push:
    branches: [refatoracao-arquitetura-limpa]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: docker.io
  REGISTRY_NAMESPACE: facilita
  IMAGE_NAME_TESTE: conexao-de-sorte-backend-teste
  IMAGE_NAME_PROD: conexao-de-sorte-backend

jobs:
  build-and-push:
    name: "🔨 Build e Push"
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
      deploy_type: ${{ steps.meta.outputs.deploy_type }}
      full_image: ${{ steps.meta.outputs.full_image }}
      image_name: ${{ steps.meta.outputs.image_name }}
    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: "🔍 Determinar tipo de deploy"
        id: determine
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            DEPLOY_TYPE="${{ github.event.inputs.deploy_type }}"
          else
            DEPLOY_TYPE="teste"
          fi
          echo "deploy_type=${DEPLOY_TYPE}" >> $GITHUB_OUTPUT
          echo "✅ Deploy type determined: ${DEPLOY_TYPE}"

      - name: "🏷️ Metadados da imagem"
        id: meta
        env:
          REGISTRY: ${{ env.REGISTRY }}
          REGISTRY_NAMESPACE: ${{ env.REGISTRY_NAMESPACE }}
          IMAGE_NAME_TESTE: ${{ env.IMAGE_NAME_TESTE }}
          IMAGE_NAME_PROD: ${{ env.IMAGE_NAME_PROD }}
        run: |
          set -euo pipefail
          export TZ=America/Sao_Paulo

          # Obter tipo de deploy
          DEPLOY_TYPE="${{ steps.determine.outputs.deploy_type }}"

          # Gerar timestamp no formato correto
          TIMESTAMP="$(date +'%d-%m-%Y-%H-%M')"

          # Determinar nome da imagem baseado no tipo de deploy
          if [[ "${DEPLOY_TYPE}" == "teste" ]]; then
            IMAGE_NAME="${IMAGE_NAME_TESTE}"
          else
            IMAGE_NAME="${IMAGE_NAME_PROD}"
          fi

          # Construir URL completa da imagem
          FULL_IMAGE="${REGISTRY}/${REGISTRY_NAMESPACE}/${IMAGE_NAME}:${TIMESTAMP}"

          # Definir outputs
          echo "image_tag=${TIMESTAMP}" >> $GITHUB_OUTPUT
          echo "deploy_type=${DEPLOY_TYPE}" >> $GITHUB_OUTPUT
          echo "full_image=${FULL_IMAGE}" >> $GITHUB_OUTPUT
          echo "image_name=${IMAGE_NAME}" >> $GITHUB_OUTPUT
          echo "tags=${FULL_IMAGE}" >> $GITHUB_OUTPUT

          # Log para debug
          echo "✅ Metadados gerados:"
          echo "   - Deploy Type: ${DEPLOY_TYPE}"
          echo "   - Image Tag: ${TIMESTAMP}"
          echo "   - Image Name: ${IMAGE_NAME}"
          echo "   - Full Image: ${FULL_IMAGE}"
          echo "   - Registry: ${REGISTRY}"
          echo "   - Namespace: ${REGISTRY_NAMESPACE}"

      - name: "🔑 Login Docker Hub"
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: "🐳 Build & Push Docker Image"
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            org.opencontainers.image.title=Conexao de Sorte Backend
            org.opencontainers.image.description=Backend da aplicação Conexão de Sorte
            org.opencontainers.image.version=${{ steps.meta.outputs.image_tag }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ github.run_started_at }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.url=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.vendor=Facilita Serviços
            deploy.type=${{ steps.meta.outputs.deploy_type }}

      - name: "✅ Verificar imagem criada"
        run: |
          echo "🔍 Verificando se a imagem foi criada com sucesso..."
          echo "📦 Imagem: ${{ steps.meta.outputs.full_image }}"
          echo "🏷️ Tag: ${{ steps.meta.outputs.image_tag }}"
          echo "🎯 Deploy Type: ${{ steps.meta.outputs.deploy_type }}"

          # Validar a configuração de porta
          echo "🔧 Validando configuração de porta para o ambiente: ${{ steps.meta.outputs.deploy_type }}"
          if [[ "${{ steps.meta.outputs.deploy_type }}" == "teste" ]]; then
            echo "🔍 Validando porta 8081 para ambiente de teste"
          else
            echo "🔍 Validando porta 8080 para ambiente de produção"
          fi

  deploy:
    name: "🚢 Deploy ${{ needs.build-and-push.outputs.deploy_type }}"
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: ${{ fromJSON('{"prod":"production","teste":"staging"}')[needs.build-and-push.outputs.deploy_type] || 'staging' }}
    env:
      DEPLOY_TYPE: ${{ needs.build-and-push.outputs.deploy_type }}
      IMAGE_TAG: ${{ needs.build-and-push.outputs.image_tag }}
      FULL_IMAGE: ${{ needs.build-and-push.outputs.full_image }}
      IMAGE_NAME: ${{ needs.build-and-push.outputs.image_name }}
    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v4

      - name: "🔐 Configurar SSH"
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p ${{ secrets.VPS_SSH_PORT }} ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts
          echo "✅ SSH configurado com sucesso"

      - name: "� Validar variáveis"
        run: |
          set -euo pipefail
          echo "🔍 Validando variáveis de deploy..."
          echo "   - DEPLOY_TYPE: [${{ env.DEPLOY_TYPE }}]"
          echo "   - IMAGE_TAG: [${{ env.IMAGE_TAG }}]"
          echo "   - FULL_IMAGE: [${{ env.FULL_IMAGE }}]"
          echo "   - IMAGE_NAME: [${{ env.IMAGE_NAME }}]"

          # Validar se todas as variáveis estão definidas
          if [[ -z "${{ env.DEPLOY_TYPE }}" ]]; then
            echo "❌ ERRO: DEPLOY_TYPE não está definido"
            exit 1
          fi
          if [[ -z "${{ env.IMAGE_TAG }}" ]]; then
            echo "❌ ERRO: IMAGE_TAG não está definido"
            exit 1
          fi

          # Validar endpoint de saúde após deploy
          echo "🔍 Validando endpoint de saúde..."
          if [[ "${{ needs.build-and-push.outputs.deploy_type }}" == "teste" ]]; then
            HEALTH_URL="http://localhost:8081/actuator/health"
            EXTERNAL_URL="https://www.conexaodesorte.com.br/teste/rest/actuator/health"
          else
            HEALTH_URL="http://localhost:8080/actuator/health"  
            EXTERNAL_URL="https://www.conexaodesorte.com.br/rest/actuator/health"
          fi

          # Aguardar aplicação estar pronta
          echo "⏳ Aguardando aplicação estar pronta..."
          for i in {1..10}; do
            if curl -f -s --connect-timeout 10 "$HEALTH_URL" >/dev/null 2>&1; then
              echo "✅ Aplicação está respondendo localmente"
              break
            fi
            echo "⏳ Tentativa $i/10 - aguardando..."
            sleep 10
          done

          # Testar acesso externo
          echo "🔍 Testando acesso externo..."
          if curl -f -s --connect-timeout 10 "$EXTERNAL_URL" >/dev/null; then
            echo "✅ Endpoint de saúde acessível externamente: $EXTERNAL_URL"
          else
            echo "⚠️ Endpoint de saúde não acessível externamente (pode ser problema de proxy)"
            echo "📋 URL testada: $EXTERNAL_URL"
          fi
          if [[ -z "${{ env.FULL_IMAGE }}" ]]; then
            echo "⚠️ FULL_IMAGE vazio - reconstruindo localmente"
            FULL_IMAGE="${{ env.REGISTRY }}/${{ env.REGISTRY_NAMESPACE }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}"
            echo "   FULL_IMAGE reconstruído: [${FULL_IMAGE}]"
            echo "FULL_IMAGE=${FULL_IMAGE}" >> $GITHUB_ENV
          fi
          echo "✅ Todas as variáveis estão válidas"

      - name: "🛑 Limpeza de containers antigos"
        run: |
          set -euo pipefail
          echo "🧹 Limpando containers antigos do tipo: ${{ env.DEPLOY_TYPE }}"

          if [[ "${{ env.DEPLOY_TYPE }}" == "teste" ]]; then
            CONTAINER_NAME="backend-teste"
          else
            CONTAINER_NAME="backend-prod"
          fi

          ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.VPS_SSH_PORT }} -o StrictHostKeyChecking=no ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} "
            set -euo pipefail
            echo '🔍 Verificando containers existentes...'

            # Parar e remover container específico
            if docker ps -a --format '{{.Names}}' | grep -q '^${CONTAINER_NAME}$'; then
              echo '🛑 Parando container: ${CONTAINER_NAME}'
              docker stop '${CONTAINER_NAME}' || true
              echo '🗑️ Removendo container: ${CONTAINER_NAME}'
              docker rm '${CONTAINER_NAME}' || true
            else
              echo '✅ Nenhum container ${CONTAINER_NAME} encontrado'
            fi
          "
          echo "✅ Limpeza concluída"

      - name: "🚀 Deploy remoto"
        run: |
          set -euo pipefail
          echo "🚀 Iniciando deploy remoto..."
          echo "   - Tipo: ${{ env.DEPLOY_TYPE }}"
          echo "   - Imagem: ${{ env.FULL_IMAGE }}"

          # Executar deploy no servidor remoto
          ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.VPS_SSH_PORT }} -o StrictHostKeyChecking=no ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} << 'REMOTE_SCRIPT'
          set -euo pipefail
          export TZ=America/Sao_Paulo

          # Variáveis passadas do GitHub Actions
          DEPLOY_TYPE="${{ env.DEPLOY_TYPE }}"
          IMAGE_TAG="${{ env.IMAGE_TAG }}"
          FULL_IMAGE_RECEIVED="${{ env.FULL_IMAGE }}"
          IMAGE_NAME="${{ env.IMAGE_NAME }}"

          echo "🔍 Variáveis recebidas no servidor:"
          echo "   - DEPLOY_TYPE: [${DEPLOY_TYPE}]"
          echo "   - IMAGE_TAG: [${IMAGE_TAG}]"
          echo "   - FULL_IMAGE_RECEIVED: [${FULL_IMAGE_RECEIVED}]"
          echo "   - IMAGE_NAME: [${IMAGE_NAME}]"

          # Validação das variáveis básicas
          if [[ -z "${DEPLOY_TYPE}" || -z "${IMAGE_TAG}" ]]; then
            echo "❌ ERRO: Variáveis básicas não foram passadas corretamente"
            echo "   DEPLOY_TYPE: [${DEPLOY_TYPE:-VAZIO}]"
            echo "   IMAGE_TAG: [${IMAGE_TAG:-VAZIO}]"
            exit 1
          fi

          # Reconstruir FULL_IMAGE sempre para garantir que está correta
          if [[ "${DEPLOY_TYPE}" == "teste" ]]; then
            FINAL_IMAGE_NAME="conexao-de-sorte-backend-teste"
            SERVER_PORT="8081"
          else
            FINAL_IMAGE_NAME="conexao-de-sorte-backend"
            SERVER_PORT="8080"
          fi

          FULL_IMAGE="docker.io/facilita/${FINAL_IMAGE_NAME}:${IMAGE_TAG}"

          echo "🔧 Imagem final construída:"
          echo "   - FINAL_IMAGE_NAME: [${FINAL_IMAGE_NAME}]"
          echo "   - FULL_IMAGE: [${FULL_IMAGE}]"
          echo "   - SERVER_PORT: [${SERVER_PORT}]"

          echo "🔑 Fazendo login no Docker Hub..."
          echo '${{ secrets.DOCKER_PASSWORD }}' | docker login -u '${{ secrets.DOCKER_USERNAME }}' --password-stdin

          echo "📥 Baixando imagem: ${FULL_IMAGE}"
          docker pull "${FULL_IMAGE}"

          echo "🌐 Criando rede Docker se não existir..."
          docker network create conexao-network 2>/dev/null || true

          echo "🚀 Iniciando deploy para: ${DEPLOY_TYPE}"
          if [[ "${DEPLOY_TYPE}" == "prod" ]]; then
            echo "🏭 Configurando ambiente de PRODUÇÃO..."

            # Configurações específicas de produção
            CONTAINER_NAME="backend-prod"
            SERVER_PORT="8080"
            # Traefik removido - acesso direto via porta

            echo "🚀 Iniciando container de produção..."
            docker run -d \
              --name "${CONTAINER_NAME}" \
              --network conexao-network \
              --restart unless-stopped \
              -p "${SERVER_PORT}:${SERVER_PORT}" \
              --health-cmd="curl -f http://localhost:${SERVER_PORT}/actuator/health || exit 1" \
              --health-interval=60s \
              --health-timeout=30s \
              --health-retries=3 \
              --health-start-period=120s \
              -e SPRING_PROFILES_ACTIVE=prod,azure \
              -e ENVIRONMENT=production \
              -e SERVER_PORT="${SERVER_PORT}" \
              -e AZURE_KEYVAULT_ENABLED=true \
              -e AZURE_KEYVAULT_ENDPOINT='${{ secrets.AZURE_KEYVAULT_ENDPOINT }}' \
              -e AZURE_CLIENT_ID='${{ secrets.AZURE_CLIENT_ID }}' \
              -e AZURE_CLIENT_SECRET='${{ secrets.AZURE_CLIENT_SECRET }}' \
              -e AZURE_TENANT_ID='${{ secrets.AZURE_TENANT_ID }}' \
              -e AZURE_KEYVAULT_FALLBACK_ENABLED=false \
              -e SPRING_DATASOURCE_URL='jdbc:mysql://conexao-mysql:3306/conexao_de_sorte?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo' \
              -e SPRING_DATASOURCE_USERNAME='${{ secrets.CONEXAO_DE_SORTE_DATABASE_USERNAME }}' \
              -e SPRING_DATASOURCE_PASSWORD='${{ secrets.CONEXAO_DE_SORTE_DATABASE_PASSWORD }}' \
              -e APP_ENCRYPTION_MASTER_PASSWORD='${{ secrets.APP_ENCRYPTION_MASTER_PASSWORD }}' \
              -e JWT_ISSUER='https://www.conexaodesorte.com.br' \
              -e JWT_AUDIENCE='conexao-de-sorte' \
              -e JWT_ALGORITHM='RS256' \
              -e TZ=America/Sao_Paulo \
              "${FULL_IMAGE}"
          else
            echo "🧪 Configurando ambiente de TESTE..."

            # Configurações específicas de teste
            CONTAINER_NAME="backend-teste"
            SERVER_PORT="8081"
            # Traefik removido - acesso direto via porta

            echo "🚀 Iniciando container de teste..."
            docker run -d \
              --name "${CONTAINER_NAME}" \
              --network conexao-network \
              --restart unless-stopped \
              -p "${SERVER_PORT}:${SERVER_PORT}" \
              --health-cmd="curl -f http://localhost:${SERVER_PORT}/actuator/health || exit 1" \
              --health-interval=60s \
              --health-timeout=30s \
              --health-retries=3 \
              --health-start-period=120s \
              -e SPRING_PROFILES_ACTIVE=prod,azure \
              -e ENVIRONMENT=staging \
              -e SERVER_PORT="${SERVER_PORT}" \
              -e AZURE_KEYVAULT_ENABLED=true \
              -e AZURE_KEYVAULT_ENDPOINT='${{ secrets.AZURE_KEYVAULT_ENDPOINT }}' \
              -e AZURE_CLIENT_ID='${{ secrets.AZURE_CLIENT_ID }}' \
              -e AZURE_CLIENT_SECRET='${{ secrets.AZURE_CLIENT_SECRET }}' \
              -e AZURE_TENANT_ID='${{ secrets.AZURE_TENANT_ID }}' \
              -e AZURE_KEYVAULT_FALLBACK_ENABLED=false \
              -e SPRING_DATASOURCE_URL='jdbc:mysql://conexao-mysql:3306/conexao_de_sorte?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo' \
              -e SPRING_DATASOURCE_USERNAME='${{ secrets.CONEXAO_DE_SORTE_DATABASE_USERNAME }}' \
              -e SPRING_DATASOURCE_PASSWORD='${{ secrets.CONEXAO_DE_SORTE_DATABASE_PASSWORD }}' \
              -e APP_ENCRYPTION_MASTER_PASSWORD='${{ secrets.APP_ENCRYPTION_MASTER_PASSWORD }}' \
              -e JWT_ISSUER='https://www.conexaodesorte.com.br' \
              -e JWT_AUDIENCE='conexao-de-sorte' \
              -e JWT_ALGORITHM='RS256' \
              -e TZ=America/Sao_Paulo \
              "${FULL_IMAGE}"
          fi

          echo "✅ Deploy concluído com sucesso!"
          echo "📦 Container: \$(docker ps --filter name=backend-${DEPLOY_TYPE} --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}')"
          REMOTE_SCRIPT

      - name: "🏥 Health check"
        run: |
          set -euo pipefail
          echo "🏥 Iniciando verificação de saúde..."

          DEPLOY_TYPE="${{ env.DEPLOY_TYPE }}"

          # Determinar URL baseada no tipo de deploy
          if [[ "${DEPLOY_TYPE}" == "prod" ]]; then
            HEALTH_URL="http://localhost:8080/rest/actuator/health"
            CONTAINER_NAME="backend-prod"
          else
            HEALTH_URL="http://localhost:8081/rest/actuator/health"
            CONTAINER_NAME="backend-teste"
          fi

          echo "🔍 Verificando saúde do container..."
          echo "   - URL: ${HEALTH_URL}"
          echo "   - Container: ${CONTAINER_NAME}"

          # Aguardar container ficar saudável (máximo 5 minutos)
          echo "⏳ Aguardando container ficar saudável..."
          for i in $(seq 1 10); do
            echo "   Tentativa ${i}/10..."

            # Verificar se o endpoint responde
            if curl -fsS --connect-timeout 10 --max-time 30 "${HEALTH_URL}" >/dev/null 2>&1; then
              echo "✅ Health check passou! Aplicação está saudável."

              # Verificar status do container via SSH
              ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.VPS_SSH_PORT }} -o StrictHostKeyChecking=no ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} "
                echo '📊 Status final do container:'
                docker ps --filter name=${CONTAINER_NAME} --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}\t{{.RunningFor}}'
                echo '🏥 Health status:'
                docker inspect ${CONTAINER_NAME} --format='{{.State.Health.Status}}' 2>/dev/null || echo 'Health check não configurado'
              "

              # Verificar saúde do Azure Key Vault
              echo "🔐 Verificando saúde do Azure Key Vault..."
              chmod +x .github/workflows/scripts/check-azure-keyvault-health.sh
              if .github/workflows/scripts/check-azure-keyvault-health.sh "${CONTAINER_NAME}"; then
                echo "✅ Azure Key Vault funcionando corretamente"
              else
                echo "⚠️ Problemas encontrados no Azure Key Vault - verificar logs acima"
                echo "::warning title=Azure Key Vault Issues::Problemas de permissões detectados"
              fi

              exit 0
            fi

            echo "   ⏳ Aguardando 10 segundos antes da próxima tentativa..."
            sleep 10
          done

          echo "❌ Health check falhou após 10 tentativas (5 minutos)"
          echo "🔍 Verificando logs do container..."
          ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.VPS_SSH_PORT }} -o StrictHostKeyChecking=no ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} "
            echo '📋 Status do container:'
            docker ps -a --filter name=${CONTAINER_NAME} --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
            echo '📝 Últimas 500 linhas do log:'
            docker logs --tail 500 ${CONTAINER_NAME} 2>&1 || echo 'Não foi possível obter logs'
          "
          exit 1

      - name: "✅ Deploy finalizado"
        run: |
          echo "🎉 Deploy realizado com sucesso!"
          echo "   - Tipo: ${{ env.DEPLOY_TYPE }}"
          echo "   - Imagem: ${{ env.FULL_IMAGE }}"
          echo "   - Tag: ${{ env.IMAGE_TAG }}"
          echo "   - Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"

  notify-failure:
    name: "🚨 Notificar Falha"
    runs-on: ubuntu-latest
    needs: [build-and-push, deploy]
    if: failure()
    steps:
      - name: "📢 Notificação de falha"
        run: |
          echo "❌ FALHA NO DEPLOY"
          echo "   - Repositório: ${{ github.repository }}"
          echo "   - Branch: ${{ github.ref_name }}"
          echo "   - Commit: ${{ github.sha }}"
          echo "   - Workflow: ${{ github.workflow }}"
          echo "   - Run ID: ${{ github.run_id }}"
          echo "   - URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo ""
          echo "🔍 Verifique os logs acima para identificar o problema."
